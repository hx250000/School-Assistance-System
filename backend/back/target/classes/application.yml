spring:
  datasource:
    url: jdbc:mysql://localhost:3306/school?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: '00000712'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
