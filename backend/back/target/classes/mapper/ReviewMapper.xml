<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.back.mapper.ReviewMapper">

    <insert id="insert">
        INSERT INTO review (
        task_id, from_user_id, to_user_id, score, content
        ) VALUES (
        #{taskId}, #{fromUserId}, #{toUserId}, #{score}, #{content}
        )
    </insert>

</mapper>