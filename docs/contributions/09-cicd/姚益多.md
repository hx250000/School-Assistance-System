# CI/CD 配置贡献说明

## 个人信息

- 姓名：姚益多
- 学号：2312190510
- 角色：前端
- 日期：2026-05-05

## 完成的工作

### 工作流相关

- 独立编写完成 `.github/workflows/ci.yml` 全部内容，包含后端与前端两个并行 job，配置触发规则、环境、依赖安装、测试、Lint、覆盖率上传全流程
- 配置 Codecov 覆盖率上传，正确区分 frontend 与 backend 两个 flag
- 编写 README.md 顶部 CI 状态与前后端覆盖率徽章代码并提交

### 代码适配

- 前端本地测试命令与 CI 完全一致，配置 `--ci` 避免卡死
- 前端代码通过 ESLint 检查，`--max-warnings 0` 零警告通过
- 前端测试覆盖率达标（>60%），成功生成 `lcov.info` 并上传 Codecov

### 可选项

- 配置 Dependabot 自动更新依赖
- 集成 CodeRabbit AI 代码审查
- 使用 act 本地验证工作流

## PR 链接

- PR #X：https://github.com/hx250000/School-Assistance-System

## CI 运行链接

- https://github.com/hx250000/School-Assistance-System/actions/runs/25307636388/job/74187037577

## 遇到的问题和解决

- 问题：前端测试在 CI 中卡住 → 解决：在 test 命令添加 `--ci` 参数，关闭 Jest 监听模式
- 问题：Codecov 无法识别前端覆盖率 → 解决：修正路径为 `frontend\campusTask`
- 问题：ESLint 本地过但 CI 失败 → 解决：统一配置文件与依赖版本，确保环境一致

## 心得体会

我独立完成了整个 CI 工作流的编写，深刻理解 GitHub Actions 前后端并行执行、自动化测试、Lint 检查与覆盖率上报的完整流程。学会了如何适配 CI 环境、处理环境差异、调试 YAML 配置，大幅提升了项目工程化与自动化部署能力。